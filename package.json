{
  "version": "0.0.8",
  "preview": true,
  "name": "csv-table-viewer-lite",
  "displayName": "CSV & Parquet Table Viewer Lite",
  "description": "Display CSV files as interactive tables inside VS Code.",
  "publisher": "psynowiec",
  "author": {
    "name": "Piotr Synowiec",
    "email": "psynowiec@gmail.com"
  },
  "repository": "https://github.com/mysiar-org/vsc-ext-csv-table-viewer",
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.63.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onCommand:csv-table-viewer.openCsvFromExplorer",
    "onCommand:csv-table-viewer.openParquetFromExplorer"
  ],
  "contributes": {
    "commands": [
      {
        "command": "csv-table-viewer.openCsvFromExplorer",
        "title": "Open CSV in Table Viewer",
        "category": "hidden"
      },
      {
        "command": "csv-table-viewer.openParquetFromExplorer",
        "title": "Open Parquet in Table Viewer",
        "category": "hidden"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "csv-table-viewer.openCsvFromExplorer",
          "when": "false"
        },
        {
          "command": "csv-table-viewer.openParquetFromExplorer",
          "when": "false"
        }
      ],
      "explorer/context": [
        {
          "command": "csv-table-viewer.openCsvFromExplorer",
          "when": "resourceExtname =~ /\\.csv/i",
          "group": "navigation"
        },
        {
          "command": "csv-table-viewer.openParquetFromExplorer",
          "when": "resourceExtname =~ /\\.parquet/i",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.63.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "parquetjs-lite": "^0.8.7"
  }
}